```python
import sys
sys.stdin = open("input.txt","rt")

def dfs(s):
    pass
if __name__ == "__main__":
    arr = [1,2,5,3,6,7,4,3] # 1,5 , 3,3 , 2,4
    target = 6
    pair = set()
    cnt = 0
    for x in arr :
        leftover = target-x
        if leftover in pair :
           print(x,leftover)
           cnt+=1
        pair.add(x)
    print(cnt)
    # 1,5,3,6,7,4,3
    # 무분별한 숫자 사이에서, 두 수를 더했을때 K가 나오는 경우가 몇개나 있는지 탐색하시오.
    # 시간은 O(N) 으로 해결하시오
    #
    # 순회를 하면서 , 현재 인덱스 번호의 값을 K에서 뺀다.
    # 뺀수가 set 자료형에 있는지 검사한다.
    # set자료형에 있으면, 가능한 경우이므로 cnt 증가한다.
    #
    # # 만약에 없으면 아직 짝이 등장 x -> set 자료형에 삽입한다.
    # 현재의 수를 set에 삽입한다.
    #
    #
```